{{partial 'dashboard/tabs'}}

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
    </tr>
  </thead>

  <tbody>
    {{#each info }}
      <tr>
        <td>{{ name }}</td>
        <td>{{ value }}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
